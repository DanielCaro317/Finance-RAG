# =============================================================================
# DEPENDENCIAS DEL PROYECTO — Asistente Cognitivo Financiero (RAG)
# =============================================================================
# NOTA AL MARGEN (para principiantes):
#   "requirements.txt" es la lista de "ingredientes" que Python necesita
#   instalar para que el proyecto funcione. Se instalan con:
#       pip install -r requirements.txt
#   Cada línea fija una versión "compatible" (~=) para evitar que una
#   actualización rompa el código sin avisar.
# =============================================================================

# ---- API Web -----------------------------------------------------------------
# FastAPI: framework moderno para APIs en Python (elegido por velocidad,
#          tipado fuerte y documentación automática /docs).
fastapi==0.115.0
# Uvicorn: servidor ASGI que ejecuta FastAPI (el "motor" que escucha HTTP).
uvicorn[standard]==0.32.0
# python-multipart: necesario para subir archivos vía /ingest.
python-multipart==0.0.12

# ---- Configuración y validación ---------------------------------------------
# pydantic: define modelos de datos con validación automática.
pydantic==2.9.2
# pydantic-settings: lee variables del .env como objeto tipado.
pydantic-settings==2.6.1
# python-dotenv: carga el archivo .env en variables de entorno.
python-dotenv==1.0.1

# ---- LLM y Embeddings --------------------------------------------------------
# openai: cliente oficial de OpenAI (GPT-4o, text-embedding-3-small...).
openai==1.54.0
# anthropic: cliente oficial de Anthropic (Claude 3.5 Sonnet).
anthropic==0.39.0
# tiktoken: tokenizador rápido (cuenta tokens para presupuestar costos).
tiktoken==0.8.0

# ---- AWS (S3, Bedrock) -------------------------------------------------------
# boto3: SDK oficial de AWS para Python.
boto3==1.35.55

# ---- Vector Stores -----------------------------------------------------------
# chromadb: base de datos vectorial local, ideal para empezar (sin servidor).
chromadb==0.5.20
# Alternativas comentadas (descomenta si las quieres usar):
# faiss-cpu==1.9.0           # Búsqueda vectorial súper rápida (sin persistencia nativa).
# pgvector==0.3.6            # Extensión PostgreSQL para vectores (producción).
# psycopg[binary]==3.2.3     # Driver PostgreSQL para Python.

# ---- Recuperación híbrida (BM25 + vectorial) --------------------------------
# rank-bm25: implementación clásica de BM25 (búsqueda léxica).
rank-bm25==0.2.2

# ---- Procesamiento de documentos --------------------------------------------
# pypdf: lectura de PDFs (texto).
pypdf==5.1.0
# unstructured: parser robusto para PDFs, DOCX, HTML, etc.
unstructured==0.16.5
# python-docx: lectura de archivos .docx.
python-docx==1.1.2
# beautifulsoup4: parsing HTML.
beautifulsoup4==4.12.3
# Pillow: manejo de imágenes (multimodal).
Pillow==11.0.0

# ---- Chunking semántico ------------------------------------------------------
# langchain-text-splitters: splitters bien probados (Recursive, Markdown...).
langchain-text-splitters==0.3.2

# ---- Evaluación RAG ----------------------------------------------------------
# ragas: framework "LLM as a Judge" (faithfulness, relevance, etc.).
ragas==0.2.6
# datasets: requerido por ragas para construir datasets de evaluación.
datasets==3.1.0

# ---- Observabilidad ----------------------------------------------------------
# langsmith: trazas de ejecución (opcional pero muy recomendable).
langsmith==0.1.143
# loguru: logging bonito y simple.
loguru==0.7.2

# ---- Testing -----------------------------------------------------------------
pytest==8.3.3
httpx==0.27.2  # Cliente HTTP usado por TestClient de FastAPI.

# =============================================================================
# PUNTOS DE CUIDADO:
#   1) Las versiones de chromadb cambian su API frecuentemente. Si actualizas
#      revisa CHANGELOG.
#   2) ragas requiere una API key de LLM para ejecutar evaluaciones.
#   3) En Windows, faiss-cpu a veces falla; por eso el default es ChromaDB.
# =============================================================================
